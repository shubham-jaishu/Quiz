<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-black text-white p-10 flex flex-col justify-center items-center">
        <h5 class="mb-10 text-2xl font-mono">Edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind ?"
                class="block mb-3 p-3 outline-none w-80 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"><%= post.content %></textarea>
            <input class="bg-yellow-500 rounded-md px-3 py-2 block w-40 cursor-pointer text-sm font-semibold text-black"
                type="submit" value="Update Post">
        </form>
    </div>
</body>

</html>