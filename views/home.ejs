<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: black;
            color: white;
        }

        .wrapper {
            position: relative;
            background: url(../images/blur_home.jpg);
            background-repeat: no-repeat;
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .popup-info {
            position: absolute;
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%) scale(.1);
            width: 500px;
            background: #fff;
            padding: 10px 25px;
            border-radius: 6px;
            opacity: 0;
            pointer-events: none;
            transition: 0.3s easy;
        }

        .popup-info.active {
            opacity: 1;
            pointer-events: auto;
            transform: translate(-50%, -50%) scale(1);
        }

        .popup-info h2 {
            font-size: 50px;
            color: #c40094;
        }

        .popup-info .info {
            display: inline-block;
            font-size: 16px;
            color: #333;
            font-weight: 500;
            margin: 4px 0;
        }

        .h-btn {
            background: linear-gradient(45deg, #007bff, #00c6ff);
            color: white;
            cursor: pointer;
            margin-top: 10px;
            width: 145px;
            height: 65px;
            border-radius: 8px;
            font-size: 1.2rem;
            font-weight: bold;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .h-btn::before {
            content: "";
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.4), transparent);
            transform: rotate(45deg);
            transition: all 0.5s ease-in-out;
        }

        .h-btn:hover::before {
            top: 100%;
            left: 100%;
        }

        .h-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

        .h-btn:active {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .h-btn span {
            display: block;
            position: relative;
            z-index: 2;
        }

        .h-btn::after {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            opacity: 0;
            transition: all 2s ease-in-out;
        }

        .h-btn:hover::after {
            opacity: 1;
        }

        .h-btn:hover {
            cursor: pointer;
            box-shadow: 0 0 1px #03e9f4, 0 0 2px #03e9f4, 0 0 5px #03e9f4, 0 0 10px #03e9f4;
        }

        .popup-info .btn-group {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-top: 1px solid #999;
            margin-top: 10px;
            padding: 15px 0 7px;
        }

        .popup-info .btn-group .info-btn {
            width: 130px;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            height: 45px;
            background: #c40094;
            border: 2px solid #c40094;
            outline: none;
            border-radius: 6px;
            text-decoration: none;
            font-size: 16px;
            color: #fff;
            font-weight: 600;
            box-shadow: 0 0 10px rgba(0, 0, 0, .1);
            cursor: pointer;
            transition: 0.5s;
        }

        .popup-info .btn-group .info-btn:nth-child(1) {
            background: transparent;
            color: #c40094;
        }

        .popup-info .btn-group .info-btn:nth-child(1):hover {
            background: #c40094;
            color: #fff;
        }

        .popup-info .btn-group .info-btn:nth-child(2):hover {
            background: #950170;
            border-color: #950170;
        }

        .main-container {
            backdrop-filter: blur(5px);
            background-color: rgba(0, 0, 0, 0.5);
            padding: 20px;
            border-radius: 10px;
            width: 540px;
            text-align: center;
            min-height: 300px;
            display: flex;
            justify-content: center;
        }

        .first-container {
            margin-bottom: 4rem;
            margin-top: 2.5rem;
        }

        .btns{
            display: flex;
            justify-content: center;
        }

        .prf{
            transition: .5s ease;
        }

        .prf:hover{
            transform: scale(1.5);
        }
    </style>
</head>

<body>
    <div class="wrapper w-full min-h-screen relative text-white">
        <div class="flex justify-end absolute top-0 right-0 m-5">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout 💀</a>
        </div>

        <div class="main-container flex flex-col gap-20">
            <div class="flex justify-center gap-3 pt-5">
                <div class="w-12 h-12 rounded-md overflow-hidden mr-5 prf">
                    <a href="/profile/upload">
                        <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>"
                            alt="Profile Picture"
                            onerror="this.onerror=null; this.src='../images/uploads/image_profile.png';">
                    </a>
                </div>

                <h3 class="text-3xl flex items-center"><span class="font-light">Hello</span>, <%= user.name %> 👋</h3>
            </div>

            <div class="second_container btns">
                <div class="flex justify-around gap-9">
                    <div class="w-1/3">
                        <a href="/create_quiz"><button class="btn text-xl font-semibold rounded-md h-btn">Add Question</button></a>
                    </div>
                    <div class="w-1/3">
                        <a href="/profile"><button class="btn text-xl font-semibold rounded-md h-btn">To Do</button></a>
                    </div>
                    <div class="w-1/3">
                        <button class="btn text-xl font-semibold rounded-md start-btn h-btn">Take Quiz</button>
                    </div>
                </div>
                


                <div class="popup-info">
                    <h2>Quiz Guide</h2>
                    <span class="info">1. Read each question carefully.</span>
                    <span class="info">2. Answer all questions; there’s no penalty for guessing.</span>
                    <span class="info">3. Stay focused; avoid distractions.</span>
                    <span class="info">4. Click 'Submit' to record your answers.</span>
                    <span class="info">5. Complete the quiz honestly.</span>

                    <div class="btn-group">
                        <button class="info-btn exit-btn">Exit</button>
                        <a href="/takequiz" class="info-btn continue-btn">Continue</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        const startBtn = document.querySelector('.start-btn')
        const exitBtn = document.querySelector('.exit-btn')
        const popupInfo = document.querySelector('.popup-info')
        const wrapper = document.querySelector('.wrapper')

        startBtn.onclick = (e) => {
            e.preventDefault();
            popupInfo.classList.add('active')
            wrapper.classList.add('active')
        }

        exitBtn.onclick = (e) => {
            // e.preventDefault();
            popupInfo.classList.remove('active')
            // wrapper.classList.remove('active')
        }
    </script>
</body>

</html>